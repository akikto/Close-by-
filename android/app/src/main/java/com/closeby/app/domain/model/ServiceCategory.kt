package com.closeby.app.domain.model

/**
 * Top-level categories Close by organizes providers into.
 * Kept as a simple enum for the base project; may grow into a richer
 * taxonomy (subcategories) in a later feature task.
 */
enum class ServiceCategory {
    VEHICLE,
    LABOUR,
    EQUIPMENT
}
