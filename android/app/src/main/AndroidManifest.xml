<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Location permission: required later for nearby-service search.
         Not requested at app-launch; requested contextually by the Explore feature. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- NOTE: No CALL_PHONE or SEND_SMS permission is declared.
         Calling and SMS are handled by launching the native Dialer / SMS app
         via implicit intents (ACTION_DIAL / ACTION_SENDTO), which do not
         require runtime permissions. See core/phone and core/sms. -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CloseBy">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CloseBy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
